---
syntax: powershell
tags: [ system-credentials, windows ]
---

// Process to delete SYSTEM credentials from credential manager

# Download PSTools.zip:
https://download.sysinternals.com/files/PSTools.zip

# Run from an admin CMD:
PsExec.exe -i -s cmd.exe

# From the new CMD window that pops up run the following:
rundll32.exe keymgr.dll, KRShowKeyMgr

Delete/modify any listed credentials from the credential manager GUI